apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-service
spec:
  replicas: {{ .Values.walletService.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: wallet-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wallet-service
    spec:
      containers:
        - name: wallet-service
          image: {{ .Values.walletService.image }}
          ports:
            - containerPort: {{ .Values.walletService.port }}
          env:
            - name: PORT
              value: {{ .Values.walletService.port | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: backpack-secrets
                  key: jwt_secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: backpack-secrets
                  key: database_url
          livenessProbe:
            httpGet:
              path: /api/v3/ping
              port: {{ .Values.walletService.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /api/v3/ping
              port: {{ .Values.walletService.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources: {{- toYaml .Values.walletService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: wallet-service
spec:
  selector:
    app.kubernetes.io/name: wallet-service
  ports:
    - port: {{ .Values.walletService.port }}
      targetPort: {{ .Values.walletService.port }}
      name: http
  type: ClusterIP
